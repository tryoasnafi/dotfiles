-- Loaded automatically by LazyVim before lazy starts. LazyVim already sets
-- sane defaults (relativenumber, clipboard=unnamedplus, etc). Personal tweaks:
local opt = vim.opt

opt.scrolloff = 8 -- keep cursor away from screen edges
opt.confirm = true -- ask to save instead of failing on :q with unsaved changes
opt.wrap = false
opt.relativenumber = true
